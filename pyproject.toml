[tool.black]
line-length = 88
target-version = ['py38']

[tool.ruff]
select = ["E", "F", "W", "I", "UP", "B", "C4"]
ignore = ["E501"] # Handled by black

line-length = 88

[tool.ruff.per-file-ignores]
"__init__.py" = ["F401"]
"tests/*" = ["B018"] # Allow print in tests
